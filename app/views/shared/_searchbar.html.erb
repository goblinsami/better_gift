<%= form_tag homes_path, method: :get do %>
  <div class="main-search-bar rounded-pill bg-white d-flex mx-auto mt-1 mb-5 w-50">
    <div class="border-end flex-grow-1 d-flex ps-4 d-flex align-items-center">
      <input type="text" placeholder="Edad" class="border-0 d-block w-100"  >
    </div>
    <div class="flex-grow-1 d-flex align-items-center">
      <input type="text" placeholder="Precio" class="ps-4 border-0 d-block w-100"  >
    </div>
      <%= select_tag(:option, options_for_select([["Todos los públicos",2],["Niños",1],["Adultos",3]], params[:option] ), class:"select") %>
    <%= submit_tag "Search", class: "btn btn-primary rounded-pill border-white border-5 px-3" %>
      <i class="fal fa-search fw-bold"></i>
    </button>
  </div>
<% end %>
